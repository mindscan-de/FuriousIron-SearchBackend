# Metadata-Index and Metadata-Search

* Maybe we want to implement also a metadata search (like filenames?)
* Maybe use the document metadata?

* we can use that metadata for ranking and filtering the documents 